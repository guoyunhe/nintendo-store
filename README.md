## Nintendo Store

```bash
# initialize modules
git submodule update --init

# update modules to latest from remote
git submodule update --remote

# add a new module
git submodule add git@github.com:guoyunhe/xbox-store.git src/modules/xbox-store
```
